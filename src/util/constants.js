const GAME_WIDTH = 1600
const GAME_HEIGHT = 900
const CENTER_X = GAME_WIDTH / 2
const CENTER_Y = GAME_HEIGHT / 2
const SIZE = 3
const BACKGROUND_COLOR = 'rgb(220,220,220)'
const CELL_WIDTH = Math.round(500 / SIZE)
const CELL_HEIGHT = Math.round(500 / SIZE)
module.exports = { GAME_WIDTH, GAME_HEIGHT, CELL_WIDTH, CELL_HEIGHT, BACKGROUND_COLOR, CENTER_X, CENTER_Y, SIZE}